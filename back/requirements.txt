# 核心科学计算库（仅锁定主版本，保证功能）
numpy>=1.26.0,<2.0.0
pandas>=3.0.0,<4.0.0
scikit-learn>=1.3.0,<2.0.0

# WEB框架（仅锁定Flask主版本，让pip自动匹配兼容依赖）
Flask>=3.1.0,<3.2.0
Flask_Cors>=4.0.0,<5.0.0

# 安全库（使用较新版本，包含安全修复）
cryptography>=42.0.0

# 工具类（仅锁定主版本）
joblib>=1.5.0,<2.0.0
psutil>=7.0.0,<8.0.0
scapy>=2.5.0,<3.0.0

# TensorFlow（可选，注释掉降低编译压力）
# tensorflow>=2.20.0,<3.0.0

# 火山引擎SDK（锁定主版本）
volcengine_python_sdk>=5.0.0,<6.0.0
httpx>=0.27.0,<0.28.0
pydantic>=2.0.0,<3.0.0